{
  "name": "mindsphere-example-backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "config": "^3.0.1",
    "date-fns": "^1.29.0",
    "express": "^4.16.4",
    "http-proxy-middleware": "^0.19.0",
    "jsonwebtoken": "^8.4.0",
    "request": "^2.88.0"
  },
  "scripts": {
    "start": "run server.js",
    "build": "",
    "test": "NODE_ENV=test jest"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "jest": "^23.6.0",
    "supertest": "^3.3.0"
  }
}
